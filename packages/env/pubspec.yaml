name: env
description: Embed environment variables into your project. 
private: true
publish_to: none

environment:
  sdk: ^3.3.0

# Add regular dependencies here.
dependencies:
  analyzer: ^6.4.1
  build: ^2.4.1
  source_gen: ^1.5.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
