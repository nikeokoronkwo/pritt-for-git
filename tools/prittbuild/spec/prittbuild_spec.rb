# frozen_string_literal: true

RSpec.describe Prittbuild do
  it "has a version number" do
    expect(Prittbuild::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
